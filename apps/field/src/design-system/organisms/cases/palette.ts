/**
 * The case screens' tokens now live in design-system/tokens/cases.ts; this re-export keeps
 * existing imports working.
 */
export {
  caseDevanagari,
  caseFonts,
  caseInter,
  caseMetrics,
  casePalette,
  caseType,
  type CaseColor,
  type CaseFace,
  type CaseTypeRole,
  type CaseWeight,
} from '../../tokens/cases';
