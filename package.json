{
  "name": "ada-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@10.9.8",
  "engines": {
    "node": ">=22.0.0"
  },
  "workspaces": [
    "apps/web",
    "apps/field",
    "libs/ts/*"
  ],
  "scripts": {
    "build:libs": "npm run build -w @ada/shared -w @ada/api-types",
    "dev": "npm run build:libs && npm run dev -w @ada/web",
    "build": "npm run build:libs && npm run build -w @ada/web",
    "build:app": "npm run build:libs && npm run build -w @ada/field",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run build:libs && npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present",
    "api:types": "node scripts/gen-api-types.mjs",
    "api:types:check": "npm run api:types && git diff --exit-code -- libs/ts/api-types"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  },
  "overrides": {
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "@types/react": "~19.2.18",
    "@types/react-dom": "~19.2.7",
    "openapi-typescript": {
      "typescript": "$typescript"
    }
  }
}
